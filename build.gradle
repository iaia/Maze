buildscript {
    ext.kotlin_version = "1.7.10"
}

plugins {
    id "signing"
    id "maven-publish"
}

allprojects {
    repositories {
        mavenCentral()
    }

    signing {
        sign publishing.publications
    }
}

group = "dev.iaiabot.maze"
version = "1.0-SNAPSHOT"
