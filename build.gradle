buildscript {
    ext.kotlin_version = "1.7.10"
}

plugins {
    id "signing"
    id "maven-publish"
}

allprojects {
    repositories {
        mavenCentral()
    }

}

group = "dev.iaiabot.maze"
version = "1.0"

signing {
    sign publishing.publications
}

