buildscript {
    ext.kotlin_version = "1.7.10"
}

allprojects {
    repositories {
        mavenCentral()
    }
}

group = "dev.iaiabot.maze"
version = "1.0"

apply from: "${rootDir}/scripts/publish-root.gradle"
